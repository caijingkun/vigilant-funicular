# =====================================================================
# 基于AI的高校教室能耗预测与智能节能决策系统 - 依赖清单
# 本文件供 Streamlit Community Cloud (share.streamlit.io) 自动安装依赖
# Python 版本要求：>=3.9, <3.13 （Streamlit Cloud 官方支持版本范围）
# =====================================================================

# ---------- Web 框架 ----------
streamlit==1.40.0

# ---------- 数据处理 ----------
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1

# ---------- 可视化 ----------
matplotlib==3.9.2
Pillow==10.4.0

# ---------- 机器学习 ----------
scikit-learn==1.5.2
joblib==1.4.2

# ---------- 说明 ----------
# 本项目默认使用 scikit-learn 的 RandomForest 作为预测模型；
# 如需启用更重的 LSTM(PyTorch)，可在 requirements 中追加：
#   torch==2.4.1
# 但请注意：Streamlit Cloud 冷启动安装 torch 可能接近或超过资源限制，
# 且本项目在 torch 缺失时会自动回退到 RandomForest，功能不受影响，
# 因此默认不安装 torch。
